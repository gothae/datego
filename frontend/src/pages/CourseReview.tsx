import * as React from 'react';
import {View, Text, Image, ScrollView, StyleSheet} from 'react-native';
import {Button} from '@react-native-material/core';

function CourseReview({navigation, route}: props){
    const spotId =
}